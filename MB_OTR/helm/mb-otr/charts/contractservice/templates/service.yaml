apiVersion: v1
kind: Service
metadata:
  name: {{ include "contractservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "contractservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contractservice.selectorLabels" . | nindent 4 }}
