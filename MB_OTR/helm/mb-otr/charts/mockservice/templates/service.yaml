apiVersion: v1
kind: Service
metadata:
  name: {{ include "mockservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mockservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mockservice.selectorLabels" . | nindent 4 }}
