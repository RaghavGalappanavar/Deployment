apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderplacementservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "orderplacementservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderplacementservice.selectorLabels" . | nindent 4 }}
