apiVersion: v1
kind: Service
metadata:
  name: {{ include "purchaserequestservice.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "purchaserequestservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "purchaserequestservice.selectorLabels" . | nindent 4 }}
